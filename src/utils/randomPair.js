/**
 * returns randomly generated index pairs
 * while ensuring that no pair is the same as the other or as the center
 * @param {Number} centerRow
 * @param {Number} centerCol
 */
export default function generateRandomPair(centerRow, centerCol) {}
